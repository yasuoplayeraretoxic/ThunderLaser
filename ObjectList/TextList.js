let TLTitle = new Text({
    x: 0 * scale,
    y: -80 * scale,
    text: 'THUNDER LASER',
    fontSize: 42 * scale
});
let yellow = new Text({
    x: -140 * scale,
    y: -15 * scale,
    text: 'YELLOW',
    fontSize: 21 * scale
});
let pink = new Text({
    x: 0 * scale,
    y: -15 * scale,
    text: 'PINK',
    fontSize: 21 * scale
});
let blue = new Text({
    x: 140 * scale,
    y: -15 * scale,
    text: 'BLUE',
    fontSize: 21 * scale
});
let cyan = new Text({
    x: -140 * scale,
    y: 45 * scale,
    text: 'CYAN',
    fontSize: 21 * scale
});
let purple = new Text({
    x: 0 * scale,
    y: 45 * scale,
    text: 'PURPLE',
    fontSize: 21 * scale
});
let gray = new Text({
    x: 140 * scale,
    y: 45 * scale,
    text: 'GRAY',
    fontSize: 21 * scale
});
let castella = new Text({
    x: -140 * scale,
    y: 105 * scale,
    text: 'CASTELLA',
    fontSize: 21 * scale
});
let blackWhite = new Text({
    x: 0 * scale,
    y: 105 * scale,
    text:"ZEBRA",
    fontSize: 21 * scale
});
let howToPlay = new Text({
    x: 0 * scale,
    y: -170 * scale,
    text: 'HOW TO PLAY：MOVE YOUR MOUSE AND TRY TO AVOID ALL SHOTS',
    fontSize: 10 * scale
});
let chooseMod = new Text({
    x: 0 * scale,
    y: -75 * scale,
    text: 'CHOOSE THE MODE\nYOU WANT',
    fontSize: 30 * scale
});
let chooseColorComb = new Text({
    x: 0 * scale,
    y: -105 * scale,
    text: 'CHOOSE THE COLOR\nCOMBINATION YOU WANT',
    fontSize: 30 * scale
});
let gamingScoreCounter = new Text({
    x: 70 * scale,
    y: -162.5 * scale,
    text: 'SCORE：0',
    fontSize: 15 * scale,
});
let gamingMvp = new Text({
    x: -70 * scale,
    y: -162.5 * scale,
    text: 'MVP：0',
    fontSize: 15 * scale,
});
let gamingDifficulty = new Text({
    x: 0 * scale,
    y: 166 * scale,
    text: 'difficulty：',
    fontSize: 15 * scale
});
let producerList = new Text({
    x: 0 * scale,
    y: -120 * scale,
    text: 'CODE\n\nBEENYAN\nJTT.YANG\n\nART\n\nY.DOOOG\nJTT.YANG',
    fontSize: 30 * scale
});
let quitGame = new Text({
    x: 0 * scale,
    y: -10 * scale,
    text: 'ARE YOU SURE TO\nQUIT THE GAME',
    fontSize: 30 * scale
});
let choosePattern = new Text({
    x: 0 * scale,
    y: -85 * scale,
    text: 'CHOOSE THE PATTERN\nYOU WANT',
    fontSize: 30 * scale
});
let menuOrAgainScoreCounter = new Text({
    x: 0 * scale,
    y: -15 * scale,
    text: 'SCORE：0',
    fontSize: 40 * scale,
});
let menuOrAgainMvp = new Text({
    x: 0 * scale,
    y: -70 * scale,
    text: 'MVP：0',
    fontSize: 50 * scale,
});
let easterEgg = new Text({
    x: 0 * scale,
    y: -170 * scale,
    text: 'ji3vu3cj0 su3 xji6m/3vm0',
    fontSize: 10 * scale
});
let homeTextList = [TLTitle];
let chooseModeTextList = [howToPlay, chooseMod];
let chooseColorTextList = [chooseColorComb,yellow, pink, blue, cyan, purple, gray, castella,blackWhite];
let gamingTextList = [gamingScoreCounter, gamingMvp, gamingDifficulty];
let producerListTextList = [producerList];
let quitGameTextList = [quitGame];
let choosePatternTextList = [choosePattern]
let menuOrAgainTextList = [menuOrAgainScoreCounter, menuOrAgainMvp]